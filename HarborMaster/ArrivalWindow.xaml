<Window x:Class="HarborMaster.ArrivalWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HarborMaster"
        mc:Ignorable="d"
        Title="Input Data Kedatangan Kapal" Height="450" Width="800">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>

        <Label Content="INPUT DATA KAPAL BARU" Grid.Row="0" Grid.ColumnSpan="2" FontWeight="Bold" FontSize="20" Margin="0,0,0,20"/>

        <Label Content="1. Nama Kapal:" Grid.Row="1" Grid.Column="0" Margin="0,10"/>
        <TextBox Name="TxtShipName" Grid.Row="1" Grid.Column="1" Margin="10,10,0,10" TextChanged="TxtShipName_TextChanged"/>

        <Label Content="2. Panjang Kapal (m):" Grid.Row="2" Grid.Column="0" Margin="0,10"/>
        <TextBox Name="TxtSize" Grid.Row="2" Grid.Column="1" Margin="10,10,0,10"/>

        <Label Content="3. Draft Kapal (m):" Grid.Row="3" Grid.Column="0" Margin="0,10"/>
        <TextBox Name="TxtDraft" Grid.Row="3" Grid.Column="1" Margin="10,10,0,10"/>

        <Label Content="4. ETA (Tgl/Waktu):" Grid.Row="4" Grid.Column="0" Margin="0,10"/>
        <DatePicker Name="DtpETA" Grid.Row="4" Grid.Column="1" Margin="10,10,0,10"/>

        <TextBlock Name="TxtStatusMessage" Grid.Row="5" Grid.ColumnSpan="2" Foreground="Red" FontWeight="SemiBold" Margin="0,20,0,0" TextWrapping="Wrap"/>

        <StackPanel Orientation="Horizontal" Grid.Row="6" Grid.ColumnSpan="2" HorizontalAlignment="Right" Margin="0,30,0,0">
            <Button Content="Cek &amp; Alokasi" Name="BtnAllocate" Click="BtnAllocate_Click" Padding="20,10" Margin="0,0,15,0"/>
            <Button Content="Batal" IsCancel="True" Padding="20,10"/>
        </StackPanel>
    </Grid>
</Window>